this is a simple text file
location of the file is {{location}}
local_ip of server is {{ansible_default_ipv4["address"]}}
the node name {{ansible_nodename}}
the OS family is {{ansible_os_family}}
the host domain is  {{ansible_domain}}
